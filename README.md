# BigData
Predictive Maintenance for Industrial Equipment

# HOW TO
For now just set up the docker enviroment with 
``` docker-compose up --build -d ``` then connect to the flask UI from the web page (http://localhost:5000/) and start the simulation.
